image: gmantaos/defold:latest
stages:
  - build

# test:
#   script:
#   - npm i

build-web:
  type: deploy
  stage: build
  script:
  - bob -h
  - echo "----------------------"
  - echo "Running bob.jar - resolving dependencies"
  - bob resolve
  - echo "Running bob.jar - building"
  - bob build -p js-web --binary-output public -br report
#   only:
#   - dev
  artifacts:
    paths:
    - report
    - public

# production:
#   type: deploy
#   stage: production
#   script:
#   - gem install dpl
#   - dpl --provider=heroku --app=kondr1 --api-key=$HEROKU_API_KEY
#   only:
#   - master
